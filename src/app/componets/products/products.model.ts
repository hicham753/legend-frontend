export class products{
    constructor(public id: number, public name: string, public imagePath: string,
         public price: number, public discountedPrice: number, public quantity: number){

    }
}